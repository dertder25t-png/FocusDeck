<Window x:Class="FocusDock.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FocusDock.App"
        mc:Ignorable="d"
        Title="FocusDock"
        Height="42" Width="1200"
        WindowStartupLocation="Manual"
        AllowsTransparency="True"
        Background="#66000000"
        Topmost="True">
    <Window.Resources>
        <Style TargetType="Border" x:Key="DockPillStyle">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="#AA111111"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <ItemsPanelTemplate x:Key="HorizontalItemsPanel">
            <StackPanel Orientation="Horizontal"/>
        </ItemsPanelTemplate>
        <ItemsPanelTemplate x:Key="VerticalItemsPanel">
            <StackPanel Orientation="Vertical"/>
        </ItemsPanelTemplate>
    </Window.Resources>
    <Grid x:Name="Root" Margin="4" Opacity="0.98">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Collapsed ribbon area; expands on mouse enter -->
        <Border x:Name="Dock" Style="{StaticResource DockPillStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Workspace and Focus toggles -->
                <StackPanel x:Name="PanelLeft" Orientation="Horizontal" Margin="8,0" VerticalAlignment="Center">
                    <Button x:Name="BtnFocus" Content="Focus" Padding="8,4" Margin="0,0,6,0"/>
                    <Button x:Name="BtnLayouts" Content="Layouts" Padding="8,4"/>
                </StackPanel>

                <!-- Center: Open windows grouped list (simplified) -->
                <ItemsControl x:Name="GroupsList" Grid.Column="1" ItemsSource="{Binding WindowGroups}" ItemsPanel="{StaticResource HorizontalItemsPanel}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource DockPillStyle}" Margin="4,0" Padding="6,2">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding GroupName}" Foreground="#EEE" Margin="0,0,6,0"/>
                                    <ItemsControl ItemsSource="{Binding Windows}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Click="OnWindowItemClick" Padding="0" Margin="0" Background="Transparent" BorderThickness="0">
                                                    <Border Margin="2,0" Padding="6,2" CornerRadius="6">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="#222"/>
                                                                <Setter Property="BorderBrush" Value="#444"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                                        <Setter Property="Background" Value="#2a3d1f"/>
                                                                        <Setter Property="BorderBrush" Value="#6fbf4a"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Ellipse Width="6" Height="6" Fill="#66FFFFFF" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{Binding Title}" Foreground="#DDD" Margin="4,0,0,0"/>
                                                        </StackPanel>
                                                    </Border>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Right: Clock / Peek area placeholder -->
                <StackPanel x:Name="PanelRight" Grid.Column="2" Orientation="Horizontal" Margin="8,0" VerticalAlignment="Center">
                    <Button x:Name="BtnCalendar" Content="📅" Padding="8,4" Margin="0,0,6,0" ToolTip="Calendar &amp; Assignments"/>
                    <Button x:Name="BtnTodos" Content="✓" Padding="8,4" Margin="0,0,6,0" ToolTip="To-Do List"/>
                    <Button x:Name="BtnStudySession" Content="⏱" Padding="8,4" Margin="0,0,6,0" ToolTip="Study Session Timer"/>
                    <Button x:Name="BtnWorkspace" Content="Workspace" Padding="8,4" Margin="0,0,6,0"/>
                    <Button x:Name="BtnDock" Content="Dock" Padding="8,4" Margin="0,0,6,0"/>
                    <Button x:Name="BtnAutomations" Content="Automations" Padding="8,4" Margin="0,0,6,0"/>
                    <Button x:Name="BtnReminders" Padding="8,4" Margin="0,0,6,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Reminders" Foreground="#EEE"/>
                            <Border Background="#444" CornerRadius="8" Margin="6,0,0,0" Padding="6,2">
                                <TextBlock x:Name="TxtRemindersCount" Text="0" Foreground="#EEE"/>
                            </Border>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnSettings" Content="⚙" Padding="8,4" Margin="0,0,6,0" ToolTip="Settings"/>
                    <TextBlock x:Name="TxtClock" Foreground="#EEE" Text="--:--"/>
                </StackPanel>

                <!-- Automation preview banner -->
                <Border x:Name="AutomationBanner" Grid.ColumnSpan="3" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" Padding="8,4" Background="#992a2a2a" CornerRadius="6" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtAutomationMessage" Foreground="#EEE" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtAutomationCountdown" Foreground="#EEE" Margin="0,0,8,0"/>
                        <Button x:Name="BtnAutomationUndo" Content="Undo" Padding="8,2"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
