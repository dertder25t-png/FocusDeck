<Window x:Class="FocusDock.App.AutomationsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Automation Rules - FocusDeck" 
        Width="900" Height="650"
        MinWidth="800" MinHeight="550"
        Background="#0F0F10" 
        Foreground="#E8EAED"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    
    <Window.Resources>
        <!-- Colors matching app theme -->
        <SolidColorBrush x:Key="AccentBrush" Color="#7B5FFF"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#9176FF"/>
        <SolidColorBrush x:Key="DarkBg1" Color="#0F0F10"/>
        <SolidColorBrush x:Key="DarkBg2" Color="#1C1C1F"/>
        <SolidColorBrush x:Key="DarkBg3" Color="#1D1E20"/>
        <SolidColorBrush x:Key="DarkBg4" Color="#27292D"/>
        <SolidColorBrush x:Key="StrokeBrush" Color="#3F4550"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#E8EAED"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#AAB2BE"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#F44336"/>
    </Window.Resources>
    
    <Border Background="{StaticResource DarkBg1}" Padding="0">
        <Grid>
            <!-- Header -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header Bar -->
            <Border Grid.Row="0" 
                    Background="{StaticResource DarkBg2}" 
                    BorderBrush="{StaticResource StrokeBrush}" 
                    BorderThickness="0,0,0,1"
                    Padding="24,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Automation Rules"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Automatically trigger actions based on time, WiFi network, or app usage"
                                   FontSize="13"
                                   Foreground="{StaticResource TextSecondary}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <Button x:Name="BtnAddRule"
                            Grid.Column="1"
                            Content="＋ New Rule"
                            Background="{StaticResource AccentBrush}"
                            Foreground="White"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Padding="20,12"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="OnAddRuleClick">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="8"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Rules List -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="24,20">
                <StackPanel x:Name="RulesList">
                    <!-- Rules will be added dynamically -->
                </StackPanel>
            </ScrollViewer>
            
            <!-- Empty State (initially visible) -->
            <StackPanel x:Name="EmptyState"
                        Grid.Row="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="Visible">
                <TextBlock Text="📋"
                           FontSize="64"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16"/>
                <TextBlock Text="No automation rules yet"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimary}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Create your first rule to automate your workspace"
                           FontSize="14"
                           Foreground="{StaticResource TextSecondary}"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,24"/>
                <Button Content="Create First Rule"
                        Background="{StaticResource AccentBrush}"
                        Foreground="White"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Padding="24,12"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="OnAddRuleClick"
                        HorizontalAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
