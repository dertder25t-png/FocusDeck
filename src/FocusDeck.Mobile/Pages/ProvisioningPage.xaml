<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="FocusDeck.Mobile.Pages.ProvisioningPage"
             Title="Scan QR Code">
    <Grid RowDefinitions="*, Auto">
        <zxing:CameraBarcodeReaderView x:Name="barcodeReader"
                                       Grid.Row="0"
                                       BarcodesDetected="BarcodesDetected" />
        <VStackLayout Grid.Row="1" Padding="10">
            <Label x:Name="resultLabel"
                   Text="Scan a QR code"
                    HorizontalOptions="Center"
                    FontSize="22" />
            <Label x:Name="tenantLabel"
                   Text="Tenant: resolving…"
                   HorizontalOptions="Center"
                   FontSize="14"
                   TextColor="{DynamicResource TextSecondaryBrush}"
                   Margin="0,4,0,0" />
            <Button x:Name="toggleScannerButton"
                    Text="Start Scanner"
                    Clicked="ToggleScanner" />
        </VStackLayout>
    </Grid>
</ContentPage>
