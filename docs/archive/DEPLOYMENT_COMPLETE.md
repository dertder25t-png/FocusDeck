## Phase 0.4 Artifact Verification

- Pending: confirm the `focusdeck-server-with-spa` artifact contains the SPA under `wwwroot/` and that a deployment from that artifact successfully serves `/` with the SPA and the PAKE register/login/tenant switch path. Requires next CI run + smoke test.
